# 酒店经营模拟平台到比赛平台的重构建议

> 文档日期：2026-04-10
> 适用对象：当前 `swufe-hotelsim` 仓库
> 目标：在不推翻现有系统的前提下，把课程型酒店经营模拟平台逐步升级为可办赛、可评分、可公示、可持续迭代的比赛平台

---

## 1. 核心结论

当前仓库并不需要推倒重来。

它已经完成了最难、最昂贵、也最容易出事故的底层部分：

- 真实用户认证与角色隔离
- 多轮次业务流转
- 决策提交、模拟处理、结果生成、教师评分闭环
- 管理端治理、名册导入导出、审计、观测、告警
- 可部署、可验收、可回归的工程基础

如果项目未来改为“酒店经营模拟比赛系统”，最合理的路线不是重写，而是采用“在现有教学平台之上叠加比赛域”的增量式重构。

总体判断如下：

- 现有仓库中约 65% 到 75% 的代码可直接复用。
- 约 15% 到 20% 需要做语义改造和结构升级。
- 约 10% 到 15% 需要作为比赛核心能力重新设计。

真正需要重构的重点，不在页面壳层，而在以下 4 个地方：

1. 规则与公式体系要从“当前课程模拟公式”升级为“可版本化规则集”。
2. 评分体系要从“教师单点评分”升级为“系统评分 + 人工评分 + 可解释拆分”。
3. 数据模型要从“学期 / 班级”抽象出“赛事 / 阶段 / 分组”。
4. 展示能力要从“个人结果页”升级为“比赛看板、裁判视图、公示页、大屏页”。

---

## 2. 当前仓库可直接承接比赛化改造的基础

以下模块已经具备较高复用价值，建议尽量保留，只做语义包装或边界扩展。

| 模块 | 当前入口 | 复用判断 | 说明 |
|---|---|---|---|
| 认证与权限 | `auth.ts`、`middleware.ts`、`src/app/(auth)` | 直接复用 | 比赛平台同样需要登录、权限、角色隔离。 |
| 数据访问层 | `src/lib/dal/*` | 直接复用 | 已经形成比较清晰的服务边界，适合继续承接比赛域。 |
| 决策提交流程 | `src/app/api/decisions` | 直接复用 | 参赛队提交决策的业务本质与当前学生队伍提交相同。 |
| 模拟执行入口 | `src/app/api/simulation/run` | 直接复用 | 可以继续作为比赛轮次结算入口。 |
| 结果查询与导出 | `src/app/api/simulation/results`、`src/app/api/export`、`src/app/api/export/grades` | 直接复用 | 仅需补赛事维度和评分维度。 |
| 轮次治理 | `src/app/api/rounds`、`src/lib/dal/rounds.ts` | 直接复用 | 比赛仍然基于阶段化轮次推进。 |
| 管理治理能力 | `src/app/api/users`、`src/components/admin/*` | 直接复用 | 赛事后台、账户治理、分组治理都可以在此演化。 |
| 审计 / 观测 / 告警 | `src/lib/audit.ts`、`src/app/api/observability`、`src/app/api/alerts*` | 直接复用 | 比赛环境比教学环境更需要可追踪与可告警。 |
| 自动化测试骨架 | `vitest`、`playwright`、`e2e/*` | 直接复用 | 可继续扩展到裁判、赛事、公示、报名与大屏流。 |
| 部署与交接体系 | `vercel.json`、`.github/workflows/*`、`docs/*` | 直接复用 | 已经具备上线、验收、交接基础。 |

---

## 3. 当前仓库与比赛平台之间的主要差距

### 3.1 业务语义差距

当前系统的主语仍然偏“课程教学”：

- `Semester`
- `Class`
- `Teacher`
- `Student`

而比赛平台的主语应逐步转向：

- `Competition`
- `Stage`
- `Group` 或 `Arena`
- `Organizer`
- `Judge`
- `Contestant`
- `Spectator`

这不代表要立刻删掉旧模型，而是要先建立“比赛语义层”，再逐步把旧模型纳入兼容映射。

### 3.2 评分机制差距

当前评分更接近“教学反馈”：

- 系统先算出结果
- 教师再写 `teacherScore` 和 `teacherComment`

比赛平台需要的是：

- 系统评分
- 裁判评分
- 评分拆解
- 评分权重配置
- 评分留痕
- 异议复核能力

### 3.3 展示能力差距

当前展示更偏“学生自己看结果”，而比赛需要：

- 实时排行榜
- 阶段晋级榜
- 裁判评分页
- 公告页
- 公示页
- 大屏展示页

### 3.4 规则管理差距

当前模拟公式虽然已经被隔离在：

- `src/lib/simulation/formulas.ts`
- `src/lib/simulation/engine.ts`
- `src/lib/simulation/parameters.ts`

但还没有真正形成比赛系统所需的：

- 规则集版本号
- 每轮规则快照
- 参数快照
- 评分权重快照
- 解释卡片输出
- 人工评分挂接能力

---

## 4. 补充后的总体重构原则

建议未来所有比赛化改造都遵守以下原则。

### 4.1 先加层，不先删层

不建议一上来就把 `Semester` 改名成 `Competition`，把 `Class` 改名成 `Group`。

更稳的方式是：

- 先新增比赛域模型和服务层
- 再做兼容映射
- 最后再决定是否需要彻底迁移旧名词

这样可以最大限度保留当前已验证通过的 API、测试与管理流程。

### 4.2 先规则版本化，再公式复杂化

比赛系统最重要的不是“公式多复杂”，而是“公式可复现、可解释、可追溯”。

因此优先顺序应是：

1. 先把规则集变成独立对象
2. 先让每轮处理保留快照
3. 先把评分拆解输出成可解释结构
4. 再逐步提高公式精度

### 4.3 先兼容当前路由，再考虑页面重命名

当前前端与测试已经稳定依赖以下路由：

- `/student/*`
- `/teacher/*`
- `/admin/*`

如果未来直接大范围改成：

- `/contestant/*`
- `/organizer/*`
- `/judge/*`

会导致大量测试、文档、交接说明和外部链接一起变动。

更建议先保留旧路由，新增比赛语义别名或新入口页，再逐步迁移。

### 4.4 比赛功能优先做“可运营闭环”，不是先做“炫展示”

优先级应该是：

1. 规则集
2. 裁判评分
3. 晋级逻辑
4. 赛事运营后台
5. 公示与大屏

而不是先做大屏动画，再回头补规则和数据结构。

---

## 5. 建议的目标架构

### 5.1 领域层结构

建议把未来的比赛系统拆成 4 层。

#### 第一层：基础平台层

继续沿用当前系统已有能力：

- 登录认证
- 用户角色
- 数据库访问
- 审计日志
- 告警与运维
- 部署与 CI

#### 第二层：比赛域层

新增比赛专属概念：

- 赛事 `Competition`
- 阶段 `CompetitionStage`
- 分组 `CompetitionGroup`
- 规则集 `Ruleset`
- 裁判指派 `JudgeAssignment`
- 公告 `Announcement`
- 晋级结果 `Advancement`

#### 第三层：模拟与评分层

建立标准化规则引擎输出：

- 决策输入
- 市场结果
- 财务结果
- 品牌与 ESG 演化
- 系统评分拆解
- 裁判评分拆解
- 总分与排名

#### 第四层：展示与运营层

面向不同角色提供视图：

- 参赛队工作区
- 组织方工作区
- 裁判工作区
- 平台管理员工作区
- 公开排行榜
- 大屏展示页

### 5.2 对当前仓库的映射关系

| 目标能力 | 当前可承接入口 | 建议动作 |
|---|---|---|
| 规则引擎 | `src/lib/simulation/*` | 在原位置继续演化，不另起一套引擎。 |
| 赛事后台 | `src/components/admin/*`、`src/app/api/users` | 在 admin 基础上扩展 organizer 能力。 |
| 轮次运营 | `src/app/api/rounds`、`src/app/api/simulation/run` | 保留入口，补阶段与规则快照。 |
| 参赛队工作区 | `src/components/student/*` | 先保留 student 语义，后续再做 contestant 语义升级。 |
| 裁判评分 | `src/app/api/grading`、`src/components/teacher/teacher-grading-panel.tsx` | 从单教师评分扩展为多裁判评分。 |
| 公开展示 | 当前缺失 | 新增 `display` 或 `public` 路由层。 |

---

## 6. 需要轻改的部分

以下内容不建议重做，只建议在现有基础上增量修改。

### 6.1 角色体系

当前角色：

- `STUDENT`
- `TEACHER`
- `ADMIN`

建议未来扩展为：

- `CONTESTANT`
- `ORGANIZER`
- `JUDGE`
- `SPECTATOR`
- `ADMIN`

但为了兼容当前系统，不建议第一步就强制改枚举名。

更稳的方案是：

- 第一阶段先新增 `JUDGE`、`SPECTATOR`
- `TEACHER` 暂时兼容 `ORGANIZER`
- `STUDENT` 暂时兼容 `CONTESTANT`
- 等比赛语义完全跑通后，再评估是否做枚举重命名

### 6.2 页面结构

当前页面结构已经比较完整，建议按以下方式渐进升级：

| 当前页面 | 未来比赛语义 |
|---|---|
| `/student/dashboard` | 参赛队总览 |
| `/student/decisions` | 比赛轮次决策 |
| `/student/results` | 轮次结果与拆解 |
| `/student/rankings` | 阶段排名 |
| `/teacher/dashboard` | 组织方总览 |
| `/teacher/simulation` | 赛事轮次运营 |
| `/teacher/grading` | 裁判评分或组织方复核 |
| `/admin/dashboard` | 平台治理台 |

建议不要第一时间改 URL，而是先改页面文案、导航语义和数据来源。

### 6.3 数据模型兼容策略

短期内建议：

- `Semester` 继续承载“赛事期次”或“赛季”
- `Class` 继续承载“分组 / 赛道 / 赛区”
- `Team` 继续承载参赛队
- `Round` 继续承载轮次

在此基础上，再增加独立比赛表，逐步把语义做干净。

---

## 7. 必须重点重构的部分

### 7.1 规则集与公式层

这是未来比赛系统的核心。

建议在当前引擎基础上补齐以下能力：

- `rulesetId`
- `rulesetVersion`
- `parameterSnapshot`
- `scoringSnapshot`
- `explanationLog`
- `randomSeed`

建议继续以这些文件作为主入口：

- `src/lib/simulation/formulas.ts`
- `src/lib/simulation/engine.ts`
- `src/lib/simulation/parameters.ts`
- `src/lib/simulation/balance-test.ts`

推荐新增的结构包括：

- `src/lib/simulation/rulesets.ts`
- `src/lib/simulation/scoring.ts`
- `src/lib/simulation/explanations.ts`

这样未来即使更换老师公式，也只需要替换规则叶子函数和参数配置，而不需要推翻整个处理链。

### 7.2 评分体系

建议未来把总分拆成三段：

1. 系统经营分
2. 人工裁判分
3. 惩罚或加分项

推荐公式结构：

- `systemScore`
- `judgeScoreAverage`
- `penaltyScore`
- `finalScore`

并保留完整拆分：

- 财务表现
- 市场表现
- 品牌表现
- ESG 表现
- 风险惩罚
- 裁判维度评分

### 7.3 排名与晋级

当前仓库已有收入排名、利润排名、综合排名。

比赛版需要进一步扩展：

- 轮次排名
- 阶段排名
- 总排名
- 晋级名单
- 淘汰状态

建议新增规则对象：

- `RankingRuleSet`
- `AdvancementRule`

避免将晋级逻辑硬编码在页面或单一路由里。

### 7.4 公示与展示

这部分当前几乎还没有正式能力，需要新增：

- 赛事首页
- 公告页
- 排行榜公示页
- 大屏页
- 裁判只读页
- 历史轮次回放页

建议单独开新的公开路由层，例如：

- `src/app/display/[competitionId]/leaderboard`
- `src/app/display/[competitionId]/results`
- `src/app/display/[competitionId]/ceremony`

---

## 8. 不建议现在就做的动作

以下改动当前不建议立即执行。

### 8.1 不建议立刻全表重命名

原因：

- 会打断已有迁移、测试、文档、接口调用
- 对实际业务价值贡献不大
- 风险高于收益

### 8.2 不建议立刻重写整个前端

当前前端已经跑通完整业务链。

比赛化第一阶段应优先确保：

- 规则可配
- 评分可配
- 裁判可用
- 排名可公示

而不是先做全面视觉重制。

### 8.3 不建议立刻引入复杂随机机制

比赛系统强调公平与可复现。

建议默认：

- 弱随机
- 可关闭
- 可设 seed
- 所有结果可重放

---

## 9. 建议的分阶段落地路线

### 阶段 A：规则集独立化

目标：在当前课程平台之上建立比赛核心引擎的接口层。

建议内容：

- 新增 `ruleset` 配置对象
- 新增轮次参数快照
- 新增评分拆解结构
- 新增解释卡片输出结构
- 保持现有 API 与页面继续可用

阶段完成标志：

- 同一轮的计算结果可复现
- 不同规则集可切换
- 前端能读到结构化解释字段

### 阶段 B：裁判评分与晋级机制

目标：把比赛最关键的人工评分与晋级逻辑补齐。

建议内容：

- 新增 `Judge` 角色
- 新增 `JudgeScore` 数据模型
- 新增多裁判评分 API
- 新增裁判评分页
- 新增阶段排名与晋级逻辑

阶段完成标志：

- 多名裁判可以独立打分
- 系统分与人工分可汇总
- 晋级名单可自动生成并可追溯

### 阶段 C：赛事运营台与公开展示

目标：把系统从“能跑”升级到“能办赛”。

建议内容：

- 赛事配置台
- 阶段管理
- 分组管理
- 公告管理
- 公开排行榜
- 大屏展示页

阶段完成标志：

- 组织方可独立配置赛事
- 观众可无登录查看公示内容
- 赛中运营不依赖直接查库

### 阶段 D：正式比赛版收口

目标：把当前平台从课程产品升级为稳定比赛产品。

建议内容：

- 规则集版本化发布
- 更严格的评分审计
- 异议复核流程
- 更完整的运营监控
- 报名与邀请码体系

---

## 10. 对当前仓库最值得补充的技术优化建议

这是在原建议基础上额外补充的、与当前代码更贴近的优化点。

### 10.1 在引擎输出里保留“解释层”

建议每轮结果除了保存最终指标，还保存解释拆分，例如：

- 需求来源拆分
- 渠道贡献拆分
- 收入构成拆分
- 成本构成拆分
- 扣分项拆分
- 最终得分拆分

原因：

- 方便前端做解释卡片
- 方便裁判理解结果
- 方便未来复核
- 方便更换公式后做 A/B 对比

### 10.2 让规则参数真正变成“受控配置”

当前参数入口已经存在，但还应进一步加固：

- 参数需带版本号
- 参数需带启用状态
- 参数修改需带审计日志
- 每轮运行时必须固化快照

### 10.3 把人工评分从“教师附加字段”升级为“独立对象”

这是比赛化的关键分界线。

如果继续把人工评分只放在 `teacherScore` 上，后续会遇到这些问题：

- 无法支持多裁判
- 无法支持不同评分维度
- 无法保留评分拆解
- 无法做复核

因此应尽早把裁判评分独立建模。

### 10.4 对排名计算做“规则对象化”

建议不要把综合排名长期硬编码在 `engine.ts` 中。

更稳的做法是：

- 引擎负责生成经营结果与系统分
- 排名模块负责读取规则集并产出排名
- 晋级模块负责读取排名并产出晋级名单

这样未来若比赛改成“利润优先”或“综合分优先”，不必重写引擎。

---

## 11. 当前最推荐的下一步

结合仓库现状，最推荐的下一步不是立刻做视觉重构，而是先做下面 4 件事：

1. 把现有模拟引擎升级为“规则集 + 参数快照 + 解释输出”的比赛内核。
2. 新增裁判评分模型与 API，把人工评分从教师字段中独立出来。
3. 为比赛引入赛事层数据模型，但保持对现有 `Semester` / `Class` 流程的兼容。
4. 新增公开排行榜与赛事展示页，建立真正的“比赛对外界面”。

如果只允许先做一件事，优先级应为：

`规则集版本化 > 裁判评分 > 晋级机制 > 公示与大屏`

---

## 12. 可行性结论

从工程可行性角度看，当前项目进入“比赛化重构阶段”是完全可行的。

理由如下：

- 核心业务链已经打通
- 模拟引擎已经有独立入口
- 管理与运维基础已经具备
- 自动化测试基础已经存在
- 当前代码结构没有明显阻止比赛化扩展的硬伤

真正的关键不再是“能不能做”，而是“按什么顺序做最稳”。

最稳的答案是：

- 保留当前教学平台骨架
- 先把规则和评分体系抽象出来
- 再叠加比赛运营与公开展示层

这样既能延续现在已经完成的大量工作，也能把未来新增成本控制在可管理范围内。

---

## 13. 建议作为后续立项依据的结论摘要

可直接用于后续沟通的简化结论如下：

- 当前系统不是废稿，反而已经具备比赛平台所需的大部分基础设施。
- 后续重构重点不在“重写前端”，而在“规则版本化、裁判评分、晋级逻辑、赛事运营”。
- 建议采用增量重构，而不是推倒重来。
- 当前仓库完全适合作为比赛系统第一版的基础底座。

---

## 14. 对照当前代码后的进一步补充结论

这一节不是概念推演，而是对照当前仓库结构、Prisma 模型、API 和页面后的补充判断。

### 14.1 当前数据库层的真实边界

当前 Prisma 核心模型仍然是：

- `User`
- `Semester`
- `Class`
- `Team`
- `HotelState`
- `Round`
- `Decision`
- `RoundResult`

这意味着系统已经具备“多人、多队、多轮、可结算、可排名”的比赛底座，但还缺以下比赛级实体：

- `Ruleset`
- `JudgeScore`
- `Competition`
- `CompetitionStage`
- `Announcement`
- `Advancement`

补充判断：

- 当前模型适合先做“最小可办赛版”。
- 不适合直接一步走到“多赛事、多赛区、多阶段、多裁判全量版”。
- 最稳的做法是先在现有模型上补比赛关键字段，再新增比赛专属表。

### 14.2 当前结果层的真实短板

目前 `RoundResult` 里已经有：

- 各类经营结果指标
- `rankRevenue`
- `rankProfit`
- `rankOccupancy`
- `rankOverall`
- `teacherScore`
- `teacherComment`

但从比赛系统视角看，它仍然缺少：

- `systemScore`
- `systemScoreBreakdown`
- `judgeScoreAggregate`
- `finalScore`
- `penaltyBreakdown`
- `explanationLog`
- `rulesetVersion`

这说明当前结果表“够教学，不够办赛”。

### 14.3 当前引擎层的真实短板

当前仓库已经把公式和引擎入口拆到了：

- `src/lib/simulation/formulas.ts`
- `src/lib/simulation/engine.ts`
- `src/lib/simulation/parameters.ts`
- `src/lib/simulation/balance-test.ts`

这是非常好的基础，但目前还存在 3 个结构性短板：

1. 综合排名和部分评分逻辑仍集中在 `engine.ts` 中。
2. 参数虽然可调，但还不是“规则集对象”。
3. 结果解释还没有形成结构化输出。

因此，真正要做的不是重写引擎，而是把引擎从“可运行”升级为“可发布规则版本”。

### 14.4 当前页面层的真实短板

现在已有角色页面是：

- 学生端 `student`
- 教师端 `teacher`
- 管理端 `admin`

这说明：

- 参赛队工作区已基本成型
- 组织方工作区已基本成型
- 平台治理后台已基本成型

但目前仍然缺：

- 裁判工作区
- 公开展示页
- 比赛公告页
- 大屏页
- 晋级结果页

所以前端不需要推翻，而是需要“在现有三端之外新增两类前台”：

- `judge`
- `display` 或 `public`

---

## 15. 建议的最小增量数据模型

如果目标是尽快从课程平台升级到“最小可办赛版”，建议不要一次性新建过多表，而是按下面顺序做。

### 15.1 第一批必须补的字段或表

#### A. 在 `Round` 上补快照字段

建议新增：

- `rulesetId`
- `rulesetVersion`
- `parameterSnapshot`
- `scoringSnapshot`
- `randomSeed`

意义：

- 固化每一轮到底按哪套规则运行
- 保证复现
- 保证后续复盘和争议核查

#### B. 在 `RoundResult` 上补解释与比赛评分字段

建议新增：

- `systemScore`
- `systemScoreBreakdown`
- `penaltyScore`
- `penaltyBreakdown`
- `finalScore`
- `explanationLog`

意义：

- 不破坏当前结果读取链
- 可以直接驱动前端解释卡片
- 可以自然接入裁判评分

#### C. 新增 `JudgeScore`

建议最小结构：

- `id`
- `roundResultId`
- `judgeId`
- `score`
- `breakdown`
- `comment`
- `createdAt`
- `updatedAt`

意义：

- 让人工评分从 `teacherScore` 中独立出来
- 为多裁判和复核留出空间

#### D. 新增 `Ruleset`

建议最小结构：

- `id`
- `name`
- `version`
- `status`
- `formulaConfig`
- `scoringConfig`
- `rankingConfig`
- `createdAt`
- `updatedAt`

意义：

- 把当前“零散参数”升级为“可发布规则集”

### 15.2 第二批再补的比赛实体

在第一批跑稳之后，再补：

- `Competition`
- `CompetitionStage`
- `CompetitionJudge`
- `Announcement`
- `Advancement`

原因：

- 这些是比赛运营层能力
- 不是最先阻塞系统从“教学”升级到“办赛”的关键点

---

## 16. 建议的目录整理方案

这一节是针对“认真整理整个项目”的具体建议。

### 16.1 现有目录总体是可用的

当前目录主干已经比较清晰：

- `src/app` 承载页面与 API
- `src/components` 承载 UI 与角色工作区
- `src/lib/dal` 承载数据访问边界
- `src/lib/simulation` 承载模拟内核
- `docs` 承载部署、运维、验收文档

这说明项目并不存在“整体目录混乱到必须重搭”的问题。

### 16.2 后续建议新增的目录

建议在不破坏现有结构的前提下，逐步新增：

```text
src/
  app/
    (dashboard)/
      judge/
    display/
  components/
    judge/
    public/
  lib/
    competition/
    ranking/
    scoring/
    simulation/
      explanations.ts
      rulesets.ts
```

推荐职责如下：

- `src/lib/competition/*`
  比赛域模型、比赛状态、晋级规则、公告逻辑
- `src/lib/ranking/*`
  排名与晋级规则，不再长期耦合在引擎里
- `src/lib/scoring/*`
  系统评分、人工评分汇总、惩罚逻辑
- `src/lib/simulation/explanations.ts`
  结果解释输出
- `src/lib/simulation/rulesets.ts`
  规则集注册、版本与默认参数

### 16.3 当前最值得拆分的大文件

从当前仓库体量看，以下文件后续最值得优先拆分：

- `src/components/admin/admin-dashboard-panel.tsx`
- `src/components/admin/admin-team-roster-manager.tsx`
- `src/components/teacher/teacher-class-detail-panel.tsx`
- `src/components/teacher/teacher-simulation-panel.tsx`
- `src/components/student/student-dashboard-panel.tsx`
- `src/i18n/messages.ts`

这里的拆分建议不是为了“好看”，而是为了后续比赛化开发时降低改动碰撞：

- 把文案对象抽离
- 把图表区块抽离
- 把表格区块抽离
- 把 hook 与数据转换逻辑抽离

### 16.4 当前不建议动的目录边界

以下边界目前建议保持稳定：

- `src/lib/dal/*`
- `src/app/api/*`
- `src/lib/simulation/*`
- `prisma/*`

原因：

- 这些位置已经是当前系统最稳定的主骨架
- 后续比赛能力最好在这些骨架上增量生长

---

## 17. 建议的第一批执行任务清单

如果现在开始真正进入比赛化改造，建议按下面这批任务开工。

### 17.1 第一优先级

1. 给模拟引擎补 `ruleset` 与轮次快照能力。
2. 给 `RoundResult` 补系统评分拆解和解释输出。
3. 从 `teacherScore` / `teacherComment` 迁移到独立 `JudgeScore` 模型。

这三件事完成后，项目就会从“教学模拟系统”升级成“比赛核心内核已成型”。

### 17.2 第二优先级

1. 新增裁判端路由和评分工作区。
2. 新增公开排行榜页。
3. 新增公告与晋级结果页。

这一步完成后，项目就会从“能结算”升级成“能办赛”。

### 17.3 第三优先级

1. 新增比赛实体 `Competition` / `Stage`。
2. 把当前 `Semester` / `Class` 流程兼容映射进比赛域。
3. 新增大屏页和公开展示配置。

这一步完成后，项目就会从“单课程实验系统”升级成“真正的多赛事可运营平台”。

---

## 18. 整理后的项目现状判断

对整个项目重新梳理后的判断如下。

### 18.1 当前项目已经完成的部分

- 工程底座已经成熟
- 核心业务链已经打通
- 运营治理后台已经具备
- 模拟引擎已经具备独立演化入口
- 文档、部署、验收、回归体系已经存在

### 18.2 当前项目最明显的短板

- 比赛语义层尚未正式建模
- 规则集尚未对象化、版本化
- 人工评分仍绑定在教师单点评分模式上
- 公开展示与裁判端仍缺失

### 18.3 当前项目最容易误判的地方

最容易误判的是“看上去只是页面文案不对”，其实真正的差距不在页面，而在：

- 规则治理
- 评分治理
- 比赛实体
- 公开展示

如果只改页面名称，而不改这四块，系统仍然只是“换皮教学平台”。

---

## 19. 下一步建议

整理完整个项目后，最合适的下一步已经比较明确。

### 19.1 最推荐的下一步

直接进入“规则集版本化第一阶段”，具体就是：

1. 设计 `Ruleset` Prisma 模型。
2. 给 `Round` 增加规则与参数快照字段。
3. 给 `RoundResult` 增加系统评分拆解与解释字段。
4. 把 `engine.ts` 的综合评分产物改造成结构化输出。
5. 保持现有 student / teacher / admin 页面继续可用。

这是当前性价比最高、风险最低、又最符合比赛化目标的一步。

### 19.2 不建议马上做的下一步

目前不建议优先做：

- 全站彻底改名
- 全量前端重设计
- 大量无约束新表一次性并入
- 复杂随机机制

这些动作都容易让项目“改很多，但比赛能力并没有真正上来”。

### 19.3 如果你希望我继续直接推进代码

那最合适的开工顺序是：

1. 先落地 `Ruleset + 快照 + explanationLog`
2. 再落地 `JudgeScore`
3. 再落地 `judge` 和 `display` 两类页面骨架

这样改完之后，项目会有一个非常清晰的里程碑：

“教学版底座不丢失，比赛版核心内核正式开始落地。”
